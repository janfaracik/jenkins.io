---
layout: default
title: Jenkins - Build great things at any scale
blogrollup: true
homepage: true
css:
  - '/css/jenkins.css'
  - '/stylesheets/styles.css'
---

:ruby
  # Images should all be between 300 and 350 pixels wide, and no more than 180px tall

  supporters = site.indexpage[:supporters]
  slides = site.indexpage[:carousel]
  latestRelease = site.jenkins.stable
  users = [
    { image: "apple",   title: "Apple" },
    { image: "netflix",   title: "Netflix" },
    { image: "uber",      title: "Uber" },
    { image: "paypal",    title: "PayPal" },
    { image: "salesforce", title: "Salesforce" },
    { image: "autodesk",  title: "Autodesk" },
    { image: "amexgbt",   title: "American Express Global Business Travel" },
    { image: "ibm",      title: "IBM" },
    { image: "arm",        title: "arm.com" }
  ]
  stories = [
    {
      id: "to-deliver-amazing-products-easily",
      organisation: 'arm',
      quote: '"The extensibility of Jenkins was the prime reason we were able to make an excellent solution for our organization."',
      description: "Chaitanya Dwivedi",
      image: "https://stories.jenkins.io/static/40cae6eacf73449a4505668f2569946d/507b0/quote.webp"
    },
    {
      id: "to-produce-ultra-modern-and-sophisticated-electronic-devices",
      organisation: 'apple',
      quote: '"It\'s the secret of successful rollouts."',
      description: "Ankit Menon",
      image: "https://stories.jenkins.io/static/1e409698e31e8bf097c8392cd388fcf4/507b0/quote.webp"
    },
    {
      id: "to-keep-ibm-always-on",
      organisation: 'ibm',
      quote: '"Jenkins is a huge part of our DevOps journey. It allows us to streamline and standardize, so we can focus elsewhere."',
      description: "Alec Rieger",
      image: "https://stories.jenkins.io/static/d150eee2289987b3851f374a3636f72f/507b0/quote.webp"
    },
    {
      id: "to-make-better-recommendations",
      organisation: 'taboola',
      quote: '"Jenkins pipelines made the implementation of a very complex flow easy."',
      description: "Tidhar Klein Orbach",
      image: "https://stories.jenkins.io/static/706848b8bebd3b0b79e7b4e238c14a81/7ab40/quote.png"
    },
    {
      id: "to-build-ci-cd-that-fits-advanced-and-unique-use-cases",
      organisation: 'camunda',
      quote: '"What helped us a lot is Jenkins\' extensibility, using Jenkins plugins and shared libraries; Jenkins is more like a framework to develop CI/CD that can fit any use case"',
      description: "Ahmed AbouZaid",
      image: "https://stories.jenkins.io/static/ac30b91e34679258c32685dcd0c69961/6ac16/quote.jpg"
    },
    {
      id: "to-automate-everything-for-a-digital-transformation-journey",
      organisation: 'wipro',
      quote: '"Because of Jenkins we have reduced more than 70% of manual and maintenance work and the team can now focus on more innovative ideas. Jenkins is a reliable tool to help us get continuous feedback in the sprint cycle which helps the team deliver user stories faster."',
      description: "Sachidananda Pattnaik",
      image: "https://stories.jenkins.io/static/706848b8bebd3b0b79e7b4e238c14a81/7ab40/quote.png"
    }
  ]
  plugins = [
    { id: "pipeline-graph-view", title: "Pipeline Graph View", description: "Pipeline Graph View makes it easy to view pipeline progress and structure.", icon: 'git-network-outline', gradient1: "#dff7e4", gradient2: "#c89aff" },
    { id: "embeddable-build-status", title: "Embeddable Build Status", description: "This plugin provides customizable badges (similar to shields.io) to any website.", icon: 'shield-outline', gradient1: "#e6d9c9", gradient2: "#a57d73" },
    { id: "dark-theme", title: "Dark Theme", description: "A sleek, modern look that's easy on the eyes.", icon: 'moon-outline', gradient1: "#3c516d", gradient2: "#171c23", dark: true },
    { id: "credentials", title: "Credentials", description: "Credentials provides a secure place to store credentials.", icon: 'id-card-outline', gradient1: "#ffefae", gradient2: "#c51c2b" }
  ]

%div.overflowno
  %div.app-container
    %div.app-home-section.app-home-landing
      %div.app-home-landing__contents
        %h1
          Build great things at
          %span
            any
          scale.
        %p{:class => 'app-home-landing__contents__description'}
          The leading open source automation server, Jenkins provides thousands of plugins to support building, deploying, and automating any project.
        %div{:class => 'betty'}
          %a{:class => 'app-button app-button--primary', :href => '/download'}
            %ion-icon{:name => "download"}
            Get Jenkins
          %a{:class => 'app-button', :href => '/changelog/' + latestRelease}
            %ion-icon{:name => "sparkles"}
            What's new in Jenkins
            = latestRelease
      %div.app-home-landing__logo
        %img{:src => "/images/logos/jenkins/jenkins.svg"}

    %div.app-home-section.darkbg
      %div.logo-marquee
        %div.logo-track
          - users.shuffle.each do |user|
            = File.read("content/images/sponsors/#{user[:image]}.svg")

    :javascript
      document.addEventListener("DOMContentLoaded", () => {
        const marquees = document.querySelectorAll(".logo-marquee");

        marquees.forEach((marquee) => {
          const track = marquee.querySelector(".logo-track");
          if (!track) return;

          // Clone once to create a seamless loop
          const originals = Array.from(track.children);
          originals.forEach((node) => track.appendChild(node.cloneNode(true)));

          let x = 0;
          let singleSetWidth = 0;

          const recalc = () => {
            // After cloning once, total width is ~2x, so half is one set
            const total = track.scrollWidth;
            singleSetWidth = total / 2;
            // Keep x within bounds on resize
            if (singleSetWidth > 0) x = x % singleSetWidth;
          };

          recalc();
          window.addEventListener("resize", recalc);

          const speed = 0.7; // pixels per frame, tweak as you like

          const tick = () => {
            x -= speed;

            // Reset when we've moved one full set
            if (singleSetWidth > 0 && Math.abs(x) >= singleSetWidth) {
              x += singleSetWidth;
            }

            track.style.transform = `translate3d(${x}px, 0, 0)`;
            requestAnimationFrame(tick);
          };

          requestAnimationFrame(tick);
        });
      });




    %div.app-home-section.idkkkk
      %h2.testingtestingtodo
        Why Jenkins
      .home-items
        .home-item
          %ion-icon{:name=>"git-pull-request-outline"}
          .home-item__title
            Continuous Integration and Continuous Delivery
          .home-item__description
            As an extensible automation server, Jenkins can be used as a simple
            CI server or turned into the continuous delivery hub for any project.
        .home-item
          %ion-icon{:name=>"download-outline"}
          .home-item__title
            Easy installation
          .home-item__description
            Jenkins is a self-contained Java-based program, ready to run
            out-of-the-box, with packages for Windows, Linux, macOS and other
            Unix-like operating systems.
        .home-item
          %ion-icon{:name=>"options-outline"}
          .home-item__title
            Easy configuration
          .home-item__description
            Jenkins can be easily set up and configured via its web interface,
            which includes on-the-fly error checks and built-in help.
        .home-item
          %ion-icon{:name=>"git-network-outline"}
          .home-item__title
            Distributed
          .home-item__description
            Jenkins can easily distribute work across multiple machines,
            helping drive builds, tests and deployments across multiple
            platforms faster.

    %div.app-home-section
      .lordeeeee
        %h2
          Stories
        %p{:class => 'app-home-landing__contents__description'}
          Hear from experts on how they use Jenkins to power their workflows.
        %a.app-button.app-button--dark{:href => "https://stories.jenkins.io"}
          View stories
          %ion-icon{:name => "chevron-forward-outline"}
      .slidy
        %a.story.storyheadline{:href => 'https://stories.jenkins.io'}
        - stories.each do |story|
          %a.story{:href => 'https://stories.jenkins.io/user-story/' + story[:id] + '/'}
            .story__title
              = story[:quote]
            .story__description
              %img{:src => story[:image], :aria-hidden => false}
              = story[:description]
              for
              = File.read("content/images/sponsors/#{story[:organisation]}.svg")
              %ion-icon{:name => "chevron-forward-outline"}

    %div.app-home-section
      .lordeeeee
        %h2
          Plugins
        %p{:class => 'app-home-landing__contents__description'}
          Make Jenkins your own with thousands of plugins available to install and customize.
        %a.app-button.app-button--dark{:href => "https://plugins.jenkins.io"}
          View plugins
          %ion-icon{:name => "chevron-forward-outline"}
      .plugins-grid
        - plugins.each do |plugin|
          %a.slidycard{:class => ("slidycard--dark" if plugin[:dark]), :href => 'https://plugins.jenkins.io/' + plugin[:id] + '/', :style => '--gradient-1: ' + plugin[:gradient1] + '; --gradient-2: ' + plugin[:gradient2] + ';'}
            .slidycard__title
              %ion-icon{:name => plugin[:icon]}
              = plugin[:title]
              %ion-icon{:name => "chevron-forward-outline"}
            .slidycard__description
              = plugin[:description]
            .slidycard__image
              %img{:src => "/images/plugins/#{plugin[:id]}.png", :aria-hidden => true}

    %div.app-home-section
      = partial('blogs.html.haml')

    %div.app-home-section
      %h2
        There's a Jenkins for you
      %p{:class => 'app-home-landing__contents__description'}
        Meet your Jenkins match - a gallery of playful, community-made Jenkins variations, each with its own personality and style.
      .betty
        %a.app-button.app-button--dark{:href => "artwork"}
          Find your Jenkins
          %ion-icon{:name => "chevron-forward-outline"}
      %div.artworkpop
        - site.logo.to_a.shuffle.select { |_, logo| logo[:vector] || logo["vector"] }.first(5).each do |key, logo|
          %img.logo-thumb{:src => expand_link("images/#{logo['vector']}"), :alt => logo['name'], :loading => "lazy", :style => "rotate: 10deg"}

    %div.app-home-section
      %h2
        Get involved
      %p{:class => 'app-home-landing__contents__description'}
        Jenkins wouldn't be Jenkins without its thousands of contributors. Every contribution matters: code, documentation, localization, blog posts, artwork, meetups, and more.
      .betty
        %a.app-button.app-button--dark{:href => "participate"}
          Get involved
          %ion-icon{:name => "chevron-forward-outline"}
    = partial('thank-you-note.html.haml')

    %div.app-home-section
      %h2
        With thanks to
      %p{:class => 'app-home-landing__contents__description'}
        We thank the following organizations for their major commitments to
        support the Jenkins project.

      %ul.supporters
        - supporters.select{|sponsor| sponsor[:logo]}.each do |sponsor|
          %li
            %a{:href => sponsor[:url]}
              = File.read("content/images/sponsors/#{sponsor[:logo]}")

      %ul.supporters
        - supporters.reject{|supporter| supporter[:logo]}.each do |supporter|
          %li
            %a{:href => supporter[:url]}= supporter[:name]
